# OpenAI — primary LLM for research synthesis and recommendations
OPENAI_API_KEY=sk-your-openai-api-key

# Financial Modeling Prep — verified financial data (never fabricate metrics)
FMP_API_KEY=your-fmp-api-key

# Tavily — news, web search, and industry research
TAVILY_API_KEY=tvly-your-tavily-api-key

# Optional: override default model (defaults to gpt-4o; supports gpt-4.1 if available on your account)
# OPENAI_MODEL=gpt-4o

# Optional: use gpt-4o-mini for cost optimization
# OPENAI_MODEL=gpt-4o-mini
